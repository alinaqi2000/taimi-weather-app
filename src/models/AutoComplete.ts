export class AutoComplete {
    constructor(
        public id: number,
        public name: string,
        public region: string,
        public country: string,
        public url: string
    ) { }
}